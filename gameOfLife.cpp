#include <stdio.h>char currentGeneration[10][10];char nextGeneration[10][10];void clearArray(){	for (int i = 0; i < 10;i++)	{		for (int j = 0; j < 10; j++)			currentGeneration[i][j] = 0;	}}void printArray(){	for (int i = 0; i < 10;i++)	{		for (int j = 0; j < 10; j++)			printf("[%d][%d]=%d  ",i,j,currentGeneration[i][j]);		printf("\n");	}}void biganGame(){	printArray();	printf("Please enter a place");}void initGame() {	//Initialize array with 0	//Set 1 to 5 cells	//print board	clearArray();	printArray();	biganGame();}void playGame() {	//for each cell check the neighbours and decide what will happen in the next generation	//print board}void main() {	initGame();	playGame();}
